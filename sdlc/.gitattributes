# sdlc 技能快照目录：与上游提交 7cdfc64 保持字节级 1:1
# 纪律见仓库根 docs/adr/0001-快照移植纪律.md
#
# 禁止 git 对快照目录做任何换行符转换。core.autocrlf=true 的 Windows 环境在
# checkout / reset / 克隆时会把 LF 改写成 CRLF —— 快照就不再是上游字节的映像，
# 内核冻结守卫（tests/KernelFrozenTest）也会在干净克隆后立刻误报。
#
# 只作用于 skills/sdlc/。本插件其余目录（tests/、README.md、CLAUDE.md、
# hooks/）属于可动层，照常走仓库默认换行策略。
skills/sdlc/** -text
